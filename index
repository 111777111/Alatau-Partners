<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Alatau & Partners - Современные Юридические Решения</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet">
  <style>
    body, h1, h2, h3, h4, h5, p, ul {
      margin: 0;
      padding: 0;
      font-family: 'Montserrat', sans-serif;
    }

    .navbar {
      background: rgba(0, 0, 0, 0.6);
      transition: background 0.3s;
    }
    .navbar.scrolled {
      background: rgba(0, 0, 0, 0.9);
    }

    .hero-section {
      position: relative;
      height: 100vh;
      background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)),
                  url('img/1.jpg') center center/cover no-repeat;
    }
    .hero-section .container {
      position: relative;
      z-index: 2;
      color: #fff;
      padding-top: 100px;
    }
    .hero-section h1 {
      font-size: 3.5rem;
      font-weight: 700;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
    }
    .hero-section p.lead {
      font-size: 1.5rem;
      margin-top: 20px;
    }

    /* Остальные стили остаются без изменений */
    #about img { width: 100%; border-radius: 10px; }
    .service-card { background: #fff; border-radius: 10px; box-shadow: 0 8px 16px rgba(0,0,0,0.1); }
    /* ... остальные стили ... */

    @media (max-width: 767px) {
      .hero-section h1 { font-size: 2.5rem; }
      .hero-section p.lead { font-size: 1.2rem; }
    }
  </style>
</head>
<body>
  <!-- Навигация -->
  <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container">
      <a class="navbar-brand" href="#">Alatau & Partners</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
              aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
         <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
         <ul class="navbar-nav">
           <li class="nav-item"><a class="nav-link" href="#hero">Главная</a></li>
           <li class="nav-item"><a class="nav-link" href="#about">О Нас</a></li>
           <li class="nav-item"><a class="nav-link" href="#services">Услуги</a></li>
           <li class="nav-item"><a class="nav-link" href="#team">Команда</a></li>
           <li class="nav-item"><a class="nav-link" href="#testimonials">Отзывы</a></li>
           <li class="nav-item"><a class="nav-link" href="#contact">Контакты</a></li>
         </ul>
      </div>
    </div>
  </nav>

  <!-- Hero Section с вашим изображением -->
  <header id="hero" class="hero-section d-flex align-items-center">
    <div class="container text-center" data-aos="fade-up">
      <h1>Alatau & Partners</h1>
      <p class="lead">Масштабные и инновационные юридические решения для бизнеса и частных лиц</p>
      <a href="#contact" class="btn btn-primary btn-lg mt-4">Получить консультацию</a>
    </div>
  </header>

  <!-- Остальные секции остаются без изменений -->
  <section id="about" class="py-5 bg-light">
    <div class="container">
      <div class="row align-items-center">
         <div class="col-md-6" data-aos="fade-right">
            <img src="data:image/jpeg;base64,PLACEHOLDER_ABOUT" class="img-fluid rounded" alt="О Нас">
         </div>
         <div class="col-md-6" data-aos="fade-left">
            <h2>О Нас</h2>
            <p>Alatau & Partners — это команда экспертов с многолетним опытом в области гражданского, административного, семейного права, а также в вопросах ЖКХ и недвижимости. Мы предлагаем индивидуальные решения для каждого клиента, сочетая инновационные подходы с традиционными методами юридической поддержки.</p>
         </div>
      </div>
    </div>
  </section>

  <!-- ... остальные секции ... -->

  <!-- Футер -->
  <footer class="py-4 bg-secondary text-light">
    <div class="container text-center">
      <p class="mb-0">© 2025 Alatau & Partners. Все права защищены.</p>
    </div>
  </footer>

  <!-- Скрипты -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      AOS.init({ duration: 800, easing: 'ease-in-out', once: true });

      // Плавная прокрутка
      document.querySelectorAll('a.nav-link, a.btn').forEach(anchor => {
        anchor.addEventListener('click', function(e) {
          if (this.hash !== "") {
            e.preventDefault();
            const target = document.querySelector(this.hash);
            if (target) target.scrollIntoView({ behavior: 'smooth' });
          }
        });
      });

      // Изменение фона навигации
      window.addEventListener('scroll', function() {
        const navbar = document.querySelector('.navbar');
        navbar.classList.toggle('scrolled', window.scrollY > 50);
      });

      // Обработка формы
      document.getElementById('contactForm').addEventListener('submit', function(e) {
        e.preventDefault();
        alert('Спасибо за ваше сообщение! Мы свяжемся с вами в ближайшее время.');
        this.reset();
      });
    });
  </script>
</body>
</html>
